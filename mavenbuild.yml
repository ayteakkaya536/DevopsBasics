---
 - name: Maven Clean and Build
   hosts: all
   # become: yes
   tasks:
    - name: maven running
      command: "mvn clean install"
      #shell: "cd /var/lib/jenkins/workspace/test"
      #shell: "mvn clean install"
