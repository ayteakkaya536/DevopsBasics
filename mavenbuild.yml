---
 - name: Maven Clean and Build
   hosts: all
   # become: yes
   tasks:
    - name: maven running
      #shell: "cd /var/lib/jenkins/workspace/test"
      shell: "mvn test install"
      args:
        chdir: /var/lib/jenkins/workspace/test
